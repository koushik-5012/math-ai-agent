services:
  backend:
    container_name: math-ai-agent-backend
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "7860:7860"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
